import { authRoutes } from './auth/routes.js'
import { usersRoutes } from './users/routes.js'

// import all routes here


export const routes = {
    auth: authRoutes,
    users: usersRoutes
    //export all
}